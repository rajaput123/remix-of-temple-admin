/** Shared surface style for business profile cards — aligned with workspace shell. */
export const profileCardClass =
  "rounded-lg border border-border bg-card shadow-none";

/** Workspace typography class names (see index.css). */
export const profileTypography = {
  eyebrow: "text-workspace-eyebrow",
  title: "text-workspace-title",
  desc: "text-workspace-desc",
  section: "text-workspace-section",
  label: "text-workspace-label",
  body: "text-workspace-body",
  muted: "text-workspace-muted",
  mono: "text-workspace-mono",
} as const;
